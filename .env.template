# PasteleryApp — Environment Variables
#
# Local development:
#   1. Copy this file to .env
#   2. Adjust values as needed
#
# Vercel production:
#   Configure the same variables in Project Settings > Environment Variables.
#   Vercel injects them into process.env during build (no .env file required).

# Backend API base URL (include /api suffix)
API_URL=https://localhost:7229/api
